// ═══ RaceRound: гонка начисления очков на финише (F3, HANDOFF.md §3bu/§3bv) ═══
// Сама формула упаковки строк для upsertAnswers (одна пачка, а не цикл
// последовательных patchAnswer) видна прямо в RaceRound.tsx — tsc уже
// подтверждает форму AnswerUpsert[]. Полноценный regression-тест «сетевой
// сбой не роняет весь цикл молча» требует того же render-стенда, что и
// melody.race.test.tsx (см. комментарий там) — в проекте нет
// @testing-library/react, стенд не построен ни в этой, ни в прошлой сессии.
//
// После ревью F3 (HANDOFF §3bv) добавлены ещё два фикса того же класса,
// тоже требующие полного рендера RaceBoard с реальным useAnswers/useEffect:
//   - answersLoadedRef: не начислять 0 очков на первом рендере после
//     перезагрузки, пока useAnswers ещё не сделал ни одного успешного опроса
//     (bets=[] от того, что данные ЕЩЁ не пришли, а не от того, что ставок
//     реально нет);
//   - cancelled-флаг в цикле ретраев upsertAnswers/patchSession: опоздавшая
//     запись из старого цикла (ведущий уже переключил раунд с пульта, пока
//     мы ждали между попытками) больше не затирает melody-состояние уже
//     ДРУГОГО раунда.
import { describe, it } from 'vitest'

describe('RaceRound: гонки, требующие render-стенда (не построен ни в этой, ни в прошлых сессиях)', () => {
  it.todo('F3: сбой сети на upsertAnswers ретраится (до 3 попыток), а не роняет цикл молча и не оставляет stage не записанным')
  it.todo('после 9.58: перезагрузка проектора при allFinished=true и ещё не загруженных useAnswers не начисляет всем 0 очков (answersLoadedRef)')
  it.todo('после 9.58: переключение раунда с пульта во время цикла ретраев upsertAnswers не затирает melody-состояние следующего раунда (cancelled-флаг)')
})
