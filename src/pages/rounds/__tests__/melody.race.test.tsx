// ═══ MelodyRound: гонки, требующие полного рендера экрана ═══
// (F1, F9, F10 — HANDOFF.md §3bu)
//
// Логика самих фиксов ПРОВЕРЕНА иначе, без полного React-рендера:
//   - общий аудио-элемент/поколение (F1's playShared/stopShared) —
//     src/lib/__tests__/sharedAudio.race.test.ts, реальный код, с
//     доказательством «красный без фикса» в комментарии там.
//   - слияние коррекции startSec в тот же save, что advance() (F9) —
//     видно прямо в MelodyRound.tsx: checkReal больше не делает
//     отдельного saveMelody, только пишет в closure-переменную, которую
//     читает advance() при записи.
//
// НЕ покрыто тестом и оставлено как it.todo: F1 (removeEventListener
// 'playing' в cleanup эффектов listen/snippet, чтобы устаревший {once:true}
// листенер не выстрелил на другой стадии) и F10 (ref-guard клика «Играем»
// против устаревшей пересборки order) требуют реального DOM-события
// 'playing' на элементе, отданном playShared(), внутри РЕАЛЬНО
// смонтированного MelodyBoard с фейковым GameState/answers/teams — то есть
// полноценного render-стенда (React Testing Library + jsdom + мок
// transport/hooks), которого в этой сессии не было времени/бюджета
// построить (см. отчёт агента). Компонент уже спроектирован так, чтобы это
// было тестируемо (чистые хелперы вынесены в lib/melody.ts,
// lib/sharedAudio.ts) — рендер-стенда для HostScreen-экранов пока просто
// не существует как переиспользуемая инфраструктура (CLAUDE.md, раздел 6:
// юнит-тесты вёрстку/DOM-события не ловят, для этого нужен реальный рендер).
import { describe, it } from 'vitest'

describe('MelodyRound: гонки, требующие render-стенда (не построен в этой сессии)', () => {
  it.todo('F1: cleanup эффектов listen/snippet снимает "playing"-листенер, не оставляя его висеть на элементе')
  it.todo('F10: клик "Играем N сек" на ПРОЕКТОРЕ не откатывается устаревшей пересборкой order; пульт (админка) НЕ покрыт этим фиксом')
})
