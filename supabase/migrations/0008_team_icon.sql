-- ═══ 0008: ЗНАЧОК КОМАНДЫ (эмодзи) ═══
-- Игрок при регистрации выбирает не только цвет, но и эмодзи-иконку из
-- готового набора (lib/teamEmoji.ts). Пусто/NULL — иконки нет, ничего не
-- показываем рядом с именем (как было раньше).

alter table teams add column if not exists icon text;
